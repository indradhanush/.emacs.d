;;; spotify --- keybindings for spotify

;;; Commentary:

;;; Code:
(global-set-key (kbd "s-,") 'spotify-previous)
(global-set-key (kbd "s-.") 'spotify-next)
(global-set-key (kbd "s-/") 'spotify-playpause)

(provide 'spotify)
;;; spotify.el ends here
